<Page x:Class="PredpriyatieProject.Pages.OstatokBrigadi"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
     
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="OstatokBrigadi" 
      DataContext="{Binding firstVM, Mode=OneWay, Source={StaticResource Locator}}">
   

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.3*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="0.3*" MaxHeight="100"/>

        </Grid.RowDefinitions>
        <Border Grid.ColumnSpan="2">
            <DataGrid  />
        </Border>

                <StackPanel Orientation="Horizontal" Grid.Row="1" Grid.Column="1">
            <DatePicker SelectedDate="{Binding dateTime}"/>
        </StackPanel>
        
    </Grid>
</Page>
