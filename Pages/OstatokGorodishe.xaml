<Page x:Class="PredpriyatieProject.Pages.OstatokGorodishe"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PredpriyatieProject.Pages"  xmlns:ViewModels="clr-namespace:PredpriyatieProject.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Остаток Городище">
 
 <Page.DataContext>
        <ViewModels:FirstVM/>
    </Page.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.3*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="0.3*" MaxHeight="100"/>

        </Grid.RowDefinitions>
        <Button Content="{Binding dateTimeEnd}" Grid.ColumnSpan="2"/>
        <StackPanel Orientation="Horizontal" Grid.Row="1" Grid.Column="1">
            <DatePicker SelectedDate="{Binding dateTimeEnd, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>
    </Grid>
</Page>
